version: "3.8"
services:
  php:
    build: 
      context: ./
      dockerfile: Dockerfile
    container_name: mentes-libres
    restart: always
    volumes:
      - ./src:/var/www/html
volumes:
  html: {}